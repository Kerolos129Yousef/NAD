import 'package:flutter/material.dart';

class CountryTap extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      child: Center(child: Text("countries tap")),
    );
  }
}
