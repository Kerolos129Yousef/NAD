import 'package:flutter/material.dart';

class EyeImageTap extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.deepPurple,
      child: Center(child: Text("eye_image_tap")),
    );
  }
}
